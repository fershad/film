<header>
        <span>
            <h1>{{ title |  default: site.title }}</h1>
            <small>{{ tagline | default: site.tagline }}</small>
        </span>
        {% unless pageType == 'gallery' %}
            <nav>
                <ul>
                    <li><a href="/">&larr; Back to gallery</a></li>
                </ul>
            </nav>
        {% endunless %}
    </header>