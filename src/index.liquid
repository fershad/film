---
layout: base.liquid
---
<section id="gallery">
    {% for image in images %}
        <a href="/image/{{ image.file | slugify }}"><img src="../static/images/{{ image.file }}" alt=""></a>
    {% endfor %}
</section>
